# mardomvasl-bale

Telegraf-like minimal framework scaffold for Bale messenger.

Quick start

Install dev deps:

```bash
npm install
```

Run example (uses `ts-node`):

```bash
npm run dev
```

Build:

```bash
npm run build
```
